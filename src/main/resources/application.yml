spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/payment_service_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  flyway:
    enabled: false
    locations: classpath:db/migration

